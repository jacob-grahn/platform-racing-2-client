package pr2.character;

/** Neutral character appearance identifiers shared by native and archival rigs. */
typedef CharacterPartIds = {
	var hat:Int;
	@:optional var hats:Array<Int>;
	var head:Int;
	var body:Int;
	var feet:Int;
}
